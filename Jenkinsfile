pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Fetching code'
            }
        }

        stage('Build') {
            steps {
                echo 'Building application'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }

    }
}
